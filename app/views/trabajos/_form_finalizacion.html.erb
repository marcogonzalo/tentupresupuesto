<%= form_for @trabajo, :url => finalizar_trabajo_path(@trabajo, :format => 'json'), :method => 'put', :remote => true, :html => {:id => "finalizacion-trabajo"} do |f| %>
    <div id="modal-finalizado" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
        <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal" aria-hidden="true">x</button>
            <h3 id="myModalLabel">Finalizar trabajo</h3>
        </div>
        <div class="modal-body">
            <p>
                Estás a punto de marcar que el trabajo que contrataste ya ha sido finalizado.
                Por favor indícanos el precio final que tuvo el trabajo, para que luego puedas pasar a evaluar al proveedor.
            </p>
            <div class="control-group">
                <%= f.label :precio_final %>
                <div class="controls"><%= f.number_field :precio_final, :min => 5, :validate => true %></div>
            </div>
            <p>Esta información tiene fines estadísticos y en ningún momento se hará pública.</p>
        </div>
        <div class="modal-footer">
            <%= image_tag "tentu/generales/cargando.gif", {:class => "cargando cargando24", :id => "loading-trabajo"} %>
            <button class="btn" data-dismiss="modal" aria-hidden="true">Cerrar</button>
            <%= f.submit "Finalizar trabajo", :id => 'finalizar-trabajo-modal', :class => 'btn btn-warning' %>
        </div>
    </div>
<% end %>
