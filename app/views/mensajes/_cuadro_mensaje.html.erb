<% unless m.new_record? %>
<div class="mensaje mensaje-<%= m.usuario %>">
  <%= link_to "&times;".html_safe, mensaje_path(m), :method => 'delete', :class => 'close' %>
  <span class="tipo-usuario"><b><%= (m.usuario.to_s == tipo_usuario ? "tú" : m.usuario.to_s).camelize %></b>:</span><br>
  <p class="comentario"><%= m.comentario %></p>
  <time><%= l m.created_at, :format => :abrev %></time>
</div>
<% end %>
