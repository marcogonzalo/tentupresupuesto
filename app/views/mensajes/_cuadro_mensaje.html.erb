<% unless m.new_record? %>
<div class="mensaje mensaje-<%= m.usuario.downcase %> <%= m.usuario.to_s.downcase == tipo_usuario.downcase ? "mensaje-tu" : "mensaje-otro"%>">
	<p class="tipo-usuario pull-left">
		<b><%= (m.usuario.to_s.downcase == tipo_usuario.downcase ? "tú" : m.usuario.to_s).camelize %></b>: <span class="leido"><%= "("+m.leido+")" if m.usuario.downcase == tipo_usuario.downcase %></span>
	</p>
	<%= link_to "&times;".html_safe, mensaje_path(m), :method => 'delete', :class => 'pull-right close' %>
	<time class="pull-right">
		<%= l m.created_at, :format => :abrev %>
	</time>
	<p class="comentario">
		<%= m.comentario %>
	</p>
</div>
<div class="clearfix"></div>
<% end %>
