<%= form_for(@solicitante) do |f| %>
  <% if @solicitante.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@solicitante.errors.count, "error") %> prohibited this solicitante from being saved:</h2>

      <ul>
      <% @solicitante.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :apellido %><br />
    <%= f.text_field :apellido %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
