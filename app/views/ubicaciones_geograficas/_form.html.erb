<%= form_for(@ubicacion_geografica) do |f| %>
  <% if @ubicacion_geografica.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ubicacion_geografica.errors.count, "error") %> prohibited this ubicacion_geografica from being saved:</h2>

      <ul>
      <% @ubicacion_geografica.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :tipo %><br />
    <%= f.text_field :tipo %>
  </div>
  <div class="field">
    <%= f.label :entidad %><br />
    <%= f.text_field :entidad %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
